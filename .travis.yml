language: rust

rust:
  - 1.40.0
  - 1.41.0
  - 1.41.1
  - 1.42.0
  - 1.43.0
  - stable
  - beta
  - nightly

env:
  - WORKER_LIBRARY_FILE=$TRAVIS_BUILD_DIR/libworker.so

jobs:
  include:
    # Test Python SDK on nighlty
    - rust: nightly
      script:
        - cargo test --package py_mcai_worker_sdk
      env:
        - TEST_PYTHON_SDK=TRUE
        - WORKER_LIBRARY_FILE=$TRAVIS_BUILD_DIR/libworker.so

    # Rustfmt
    - rust: stable
      install:
        - rustup component add rustfmt-preview
      before_script:
        - cargo fmt --version
      script:
        - cargo fmt -- --check
      env:
        - CHECK_FORMAT=TRUE
        - WORKER_LIBRARY_FILE=$TRAVIS_BUILD_DIR/libworker.so

    # Clippy
    - rust: stable
      install:
        - rustup component add clippy-preview
      script:
        # Fail if clippy output contains "error:" or "warning:"
        - cargo clippy --package mcai_worker_sdk --package c_mcai_worker_sdk 2>&1 | tee ./clippy.out && ! grep -qe  "error:\|warning:" ./clippy.out
      env:
        - CHECK_SYNTAX=TRUE
        - WORKER_LIBRARY_FILE=$TRAVIS_BUILD_DIR/libworker.so

    # Clippy Python SDK
    # - rust: nightly
    #   install:
    #     - rustup component add clippy-preview
    #   script:
    #     # Fail if clippy output contains "error:" or "warning:"
    #     - cargo clippy --package py_mcai_worker_sdk 2>&1 | tee ./clippy.out && ! grep -qe  "error:\|warning:" ./clippy.out
    #   env:
    #     - CHECK_SYNTAX_PY_SDK=TRUE
    #     - WORKER_LIBRARY_FILE=$TRAVIS_BUILD_DIR/libworker.so

    # Test coverage
    - rust: nightly
      install:
        - travis_wait RUSTFLAGS="--cfg procmacro2_semver_exempt" cargo install cargo-tarpaulin --force
      script:
        - gcc -c -Wall -Werror -fpic c_mcai_worker_sdk/worker.cpp && gcc -shared -o libworker.so worker.o
        - travis_wait cargo tarpaulin --ciserver travis-ci --coveralls $TRAVIS_JOB_ID
      env:
        - ALLOW_FAILURE=TRUE
        - COVERAGE_CHECK=TRUE
        - TRAVIS_RUST_VERSION=nightly-2020-01-21
        - WORKER_LIBRARY_FILE=$TRAVIS_BUILD_DIR/libworker.so

    - rust: nightly
      script:
        - gcc -c -Wall -Werror -fpic c_mcai_worker_sdk/worker.cpp && gcc -shared -o libworker.so worker.o
        - cargo test
      env:
        - ALLOW_FAILURE=TRUE
        - WORKER_LIBRARY_FILE=$TRAVIS_BUILD_DIR/libworker.so  
  allow_failures:
    - env:
      - ALLOW_FAILURE=TRUE
      - WORKER_LIBRARY_FILE=$TRAVIS_BUILD_DIR/libworker.so  
  allow_failures:
    - env:
      - ALLOW_FAILURE=TRUE
      - COVERAGE_CHECK=TRUE
      - TRAVIS_RUST_VERSION=nightly-2020-01-21
      - WORKER_LIBRARY_FILE=$TRAVIS_BUILD_DIR/libworker.so

script:
  - gcc -c -Wall -Werror -fpic c_mcai_worker_sdk/worker.cpp && gcc -shared -o libworker.so worker.o
  - cargo test --package mcai_worker_sdk --package c_mcai_worker_sdk

cache:
  cargo: true

addons:
  apt:
    packages:
      - libssl-dev  # Required for tarpaulin
      - gcc
